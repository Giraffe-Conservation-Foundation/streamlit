# Site Assessment Dashboard Requirements

# Geospatial libraries
geopandas>=0.14.0
shapely>=2.0.0

# Satellite data access
pystac-client>=0.7.0
planetary-computer>=1.0.0
rasterio>=1.3.0

# Interactive map drawing
folium>=0.15.0
streamlit-folium>=0.15.0

# Data processing (likely already in main requirements)
numpy>=1.24.0
pandas>=2.0.0

# Visualization (likely already in main requirements)
plotly>=5.17.0
